<div class="nav">
	<a class="nav-closer" href="#">&times;</a>
	<div class="nav-header">
		<div class="nav-header-wrapper">
			<h3 class="nav-title">{{@blog.title}}</h3>
			<span class="nav-desc">{{@blog.description}}</span>
		</div>
	</div>
	<div>
		{{#foreach navigation}}
			<a href="{{url absolute="true"}}" class="nav-link nav-{{slug}}{{#if current}} nav-current{{/if}}" role="presentation">
				<span>{{label}}</span>
			</a>
		{{/foreach}}
	</div>
	{{#if @labs.subscribers}}
		<a class="subscribe-button icon-feed" href="{{@blog.url}}/subscribe/"></a>
	{{else}}
		<a class="subscribe-button icon-feed" href="{{@blog.url}}/rss/"></a>
	{{/if}}
</div>

<div class="menu-button-wrapper">
	<a class="menu-button mdi mdi-menu" href="#"></a>
</div>

<span class="nav-cover"></span>

<div class="body-opacity"></div>
